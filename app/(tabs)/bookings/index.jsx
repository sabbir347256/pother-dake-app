import React from 'react';
import { ScrollView } from 'react-native';

const bookings = () => {
    return (
        <ScrollView>
            
        </ScrollView>
    );
};

export default bookings;